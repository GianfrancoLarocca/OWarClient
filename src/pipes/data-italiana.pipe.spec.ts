import { DataItalianaPipe } from './data-italiana.pipe';

describe('DataItalianaPipe', () => {
  it('create an instance', () => {
    const pipe = new DataItalianaPipe();
    expect(pipe).toBeTruthy();
  });
});
